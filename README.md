session-background-slider
=========================
A small project to create (responsive) background images that will store their state in sessionStorage in the browser and retrieve it again on page reload.
